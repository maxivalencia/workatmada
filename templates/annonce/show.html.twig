{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ annonce.titre }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ annonce.contenue }}</td>
            </tr>
            <tr>
                <th>Datelimite</th>
                <td>{{ annonce.datelimite ? annonce.datelimite|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Dossier</th>
                <td>{{ annonce.dossier }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ annonce.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annonce_index') }}">back to list</a>

    <a href="{{ path('app_annonce_edit', {'id': annonce.id}) }}">edit</a>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}

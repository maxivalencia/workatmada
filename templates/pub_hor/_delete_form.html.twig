<form method="post" action="{{ path('app_pub_hor_delete', {'id': pub_hor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pub_hor.id) }}">
    <button class="btn">Delete</button>
</form>

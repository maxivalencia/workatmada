{% extends 'base.html.twig' %}

{% block title %}PubHor{% endblock %}

{% block body %}
    <h1>PubHor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pub_hor.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ pub_hor.image }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ pub_hor.lien }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ pub_hor.isactive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pub_hor_index') }}">back to list</a>

    <a href="{{ path('app_pub_hor_edit', {'id': pub_hor.id}) }}">edit</a>

    {{ include('pub_hor/_delete_form.html.twig') }}
{% endblock %}

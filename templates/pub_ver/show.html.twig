{% extends 'base_admin.html.twig' %}

{% block title %}PubVer{% endblock %}

{% block body %}
    <h1>PubVer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pub_ver.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ pub_ver.image }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ pub_ver.lien }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ pub_ver.isactive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pub_ver_index') }}">back to list</a>

    <a href="{{ path('app_pub_ver_edit', {'id': pub_ver.id}) }}">edit</a>

    {{ include('pub_ver/_delete_form.html.twig') }}
{% endblock %}

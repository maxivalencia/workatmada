{% extends 'base_admin.html.twig' %}

{% block title %}TypeAnnonce{% endblock %}

{% block body %}
    <h1>TypeAnnonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_annonce.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_annonce.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_annonce_index') }}">back to list</a>

    <a href="{{ path('app_type_annonce_edit', {'id': type_annonce.id}) }}">edit</a>

    {{ include('type_annonce/_delete_form.html.twig') }}
{% endblock %}

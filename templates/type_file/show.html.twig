{% extends 'base_admin.html.twig' %}

{% block title %}Type File{% endblock %}

{% block body %}
    <h1>Type File</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_file.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_file.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_file_index') }}">back to list</a>

    <a href="{{ path('app_type_file_edit', {'id': type_file.id}) }}">edit</a>

    {{ include('type_file/_delete_form.html.twig') }}
{% endblock %}
